<?xml version="1.0"?>
<launch>
    <!-- Run the map server -->
    <arg name="map" default="mapa_abierto"/>
    <node name="map_server" pkg="map_server" type="map_server" args="$(find pioneer_utils)/maps/$(arg map).yaml"/>
    </launch>
